function initSlider(parent){const slider=document.querySelector(parent);const slides=Array.from(slider.querySelectorAll(".slider__slide"));const sliderControls=Array.from(slider.querySelectorAll(".slider__control"));const pagination=slider.querySelector(".slider__pagination");const paginationButtons=Array.from(pagination.querySelectorAll(".slider__pagination-btn"));const ACTIVITY_CLASSES={slide:"slider__slide--current",btnPrev:"slider__control--prev",btnNext:"slider__control--next",btnPagination:"slider__pagination-btn--current",};let currentSlideIndex=0;const lastSlideIndex=slides.length-1;const slideSwitcher=(elements,activeClass)=>{elements.find((el)=>el.classList.contains(activeClass)).classList.remove(activeClass);elements[currentSlideIndex].classList.add(activeClass)};const switchSlide=()=>{slideSwitcher(slides,ACTIVITY_CLASSES.slide);slideSwitcher(paginationButtons,ACTIVITY_CLASSES.btnPagination)};const handleControlBtnClick=({target})=>{const{btnPrev,btnNext}=ACTIVITY_CLASSES;if(target.classList.contains(btnPrev)&&currentSlideIndex>0){currentSlideIndex--}else if(target.classList.contains(btnNext)&&currentSlideIndex<lastSlideIndex){currentSlideIndex++}else{currentSlideIndex===0?(currentSlideIndex=lastSlideIndex):(currentSlideIndex=0)}
switchSlide()};const handlePaginationBtnClick=({target})=>{const clickedBtnIndex=paginationButtons.indexOf(target);if(clickedBtnIndex<slides.length){currentSlideIndex=clickedBtnIndex;switchSlide()}};sliderControls.forEach((control)=>control.addEventListener("click",handleControlBtnClick));pagination.addEventListener("click",handlePaginationBtnClick)};initSlider(".slider")
